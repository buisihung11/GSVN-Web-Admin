import { PageContainer } from '@ant-design/pro-layout';
import React from 'react';

interface Props {}

const CourseListPage = (props: Props) => {
  return <PageContainer>Danh sách khóa học</PageContainer>;
};

export default CourseListPage;
